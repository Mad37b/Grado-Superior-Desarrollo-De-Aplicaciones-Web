		
		if(respuesta.equals("Si")) {
			
			System.out.println("Perfecto , vamos a añadir un valor cualquiera");
			
			if ( array != null  || array.length != 0) {
				 añadir = teclado.nextInt();
				// Add elements for key to array || Añadir elementos por teclado al array 
				for (int i = 0 ; i < array.length ; i++) {
					
					array[i]=añadir;
					
				}
				System.out.println("Resultado = "+ añadir);
				System.out.println("Resultado en el array= "+ array[0]);
			}
			System.out.println("programa terminado 2");
		}else {
			System.out.println("¿Quieres modificar algo ?");
			respuesta = teclado.next();
			if(respuesta.equals("Si")) {
				
				System.out.println("Perfecto , vamos a modificar un valor cualquiera");
				System.out.println("Selecciona el indice que quieras cambiar");
				for (int numeros : array) {
					
					System.out.println(contador+ " - " + array[contador]);
					contador++;
				}
				indice = teclado.next();
				System.out.println("Perfecto , has seleccionado el indice - " + indice );
				for (int i = 0 ; i < array.length ; i++ ) {
					
					
				}
				
				
				
			}
			
			System.out.println(" Se cierra el programa");
		}
		
		
		
				/**
		
		if ( array != null  || array.length != 0) {
			 añadir = teclado.nextInt();
			// Add elements for key to array || Añadir elementos por teclado al array 
			for (int i = 0 ; i < array.length ; i++) {
				
				array[i]=añadir;
				
			}
			System.out.println("Resultado = "+ añadir);
			System.out.println("Resultado en el array= "+ array[0]);
			
		}else {
			// Modificar elementos por teclado al array
			for ( int j = 0 ; j < array.length ; j++) {
				
				int resultado = añadir ; 
				
				array[j] = resultado;
					
			}
		}
		**/
		String Si = "Si" ;
		String No = "No"; 
		String entrada = "salir";